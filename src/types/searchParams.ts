export type SearchParams = {
  ids?: string,
  categoryTypeIds?: string,
  minPrice?: number,
  maxPrice?: number,
  minYear?: number,
  maxYear?: number,
  text?: string,
  limit?: number,
  offset?: number,
  sort?: string,
  sortBy?: string,
  sortDirection?: string,
}
